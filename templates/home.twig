{% extends 'base.html.twig' %}

{% block title %}
    Дашборд
{% endblock %}

{% block body %}
    <main role="main">
        <section class="jumbotron text-center" id="dashboard-helper">
            <div class="container">
                <h1 class="jumbotron-heading">Экспорт товаров в сообщество ВК</h1>
                <p class="lead text-muted">Здесь вы можете добавить новый экспорт, или изменить настройки
                    существующего</p>
                <p>
                    <a href="{{ path('step_one') }}" class="btn btn-primary my-2">Добавить экспорт</a>
                    <a href="#" class="btn btn-secondary my-2" id="close-dashboard-helper">Закрыть</a>
                </p>
            </div>
        </section>
        <div class="album py-5 bg-light">
            <div class="container">
                <div class="row">
                    <div class="col-md-4 d-flex align-items-stretch">
                        <div class="card mb-4 box-shadow">
                            <div class="card-body">
                                <h4 class="card-title">Новый экспорт</h4>
                                <p class="card-text text-muted">Авторизуйтесь во ВКонтакте и настройте передачу данных о товарах в ваше сообщество</p>
                                <a href="{{ path('step_one') }}" class="card-link">Перейти к созданию</a>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4 d-flex align-items-stretch">
                        <div class="card mb-4 box-shadow">
                            <div class="card-body">
                                <h5 class="card-title">Card title</h5>
                                <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6>
                                <p class="card-text">Some quick example text to build on the card title and make up the
                                    bulk of the card's content.</p>
                                <p class="card-text">Some quick example text to build on the card title and make up the
                                    bulk of the card's content.</p>
                                <a href="#" class="card-link">Card link</a>
                                <a href="#" class="card-link">Another link</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </main>

{% endblock %}

{% block custom_js %}
<script>
    var el = document.getElementById('dashboard-helper');
    if (+localStorage.getItem('isDashboardClosed') === 1) {
        el.style.display = 'none';
    } else {
        var closeBtn = document.getElementById('close-dashboard-helper');
        closeBtn.addEventListener('click', function (e) {
            e.preventDefault();
            jQuery(el).slideUp();
            localStorage.setItem('isDashboardClosed', '1');
        })
    }
</script>
{% endblock %}

